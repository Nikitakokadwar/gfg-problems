 vector<int> array;

            int i;

            for(i=0;i<n;i++)

            {

                array.push_back(ar1[i]);

                array.push_back(ar2[i]);

            }

            sort(array.begin(),array.end());

            int p= array[n]+array[n-1];

            return p;

